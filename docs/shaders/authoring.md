# Shader Authoring
> [!NOTE]
> Whilst shaders are typically created from an asset, they can also be made from C#.
> ```csharp
> new Shader(<ShaderSource>);
> ```

Writing a shader begins with creating a `.slang` file.